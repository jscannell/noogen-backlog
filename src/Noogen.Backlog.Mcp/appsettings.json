{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": "Loopback by default: this server holds one credential to a whole company's backlog and authenticates nobody, so exposing it is a deliberate act. ASPNETCORE_URLS or --urls overrides this.",
  "Urls": "http://127.0.0.1:5099"
}
